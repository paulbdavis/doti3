#!/usr/bin/env bash

set -x

DMENU="$(which rofi)"
PROMPT="? "
FONT='Ubuntu 12'

mode="dmenu"

while getopts ":rse:p:f:ml:w:h:" opt
do
    case $opt in
        r) mode="run";;
        s) mode="spotlight";;
        e) message="$OPTARG"
           mode="message";;
        p) PROMPT="$OPTARG";;
        f) FONT="$OPTARG";;
        m) SELECTEDBG="$BG"
           SELECTEDFG="$FG";; # match colors
        l) LINES="$OPTARG";;
        w) width="$OPTARG";;
        h) height="$OPTARG";;
        \?) "Invalid option -$OPTARG";;
        :) "-$OPTARG requires an argument"
    esac
done


if [[ "$mode" = "dmenu" ]]
then
    $DMENU -dmenu -p "$PROMPT"
elif [[ "$mode" = "run" ]]
then
    $DMENU -show "run" -fuzzy -display-run "$PROMPT"
elif [[ "$mode" = "spotlight" ]]
then
    $DMENU -modi drun,window,ssh -show drun -fuzzy -display-drun "$PROMPT" -display-window "$PROMPT" -display-ssh "$PROMPT"
else
    $DMENU -dmenu -e "$message" -separator-style none
fi
