#!/bin/bash
# Arch upgradeable packages

count=$(checkupdates | wc -l)

COLOR="#7f9f7f"
if [[ $count -gt 50 ]]
then
    COLOR="#dca3a3"
elif [[ $count -gt 10 ]]
then
    COLOR="#c3bf9f"
fi

echo "$count pending"
echo
echo "$COLOR"
